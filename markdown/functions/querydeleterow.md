# queryDeleteRow

Deletes a row within a query object.

```javascript
queryDeleteRow(query [, row])
```

```javascript
returns boolean
```
### Member Function Syntax

```javascript
query.deleteRow([row])
```
